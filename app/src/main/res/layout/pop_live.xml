<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="212dp"
        android:layout_height="680dp"
        android:background="@mipmap/live_list_bg"
        android:gravity="center"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:gravity="center"
            android:orientation="horizontal">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:singleLine="true"
                android:text="@string/live_name"
                android:textColor="@color/white"
                android:textSize="27sp" />

            <TextView
                android:id="@+id/live_count"
                android:layout_width="wrap_content"
                android:layout_height="27dp"
                android:layout_marginLeft="5dp"
                android:gravity="bottom"
                android:singleLine="true"
                android:text="（共82个）"
                android:textColor="@color/white"
                android:textSize="18sp" />
        </LinearLayout>

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@mipmap/live_list_divider"></View>

        <ListView
            android:id="@+id/live_list"
            android:layout_width="210dp"
            android:layout_height="match_parent"
            android:divider="@mipmap/live_list_divider"
            android:focusable="false"
            android:listSelector="@mipmap/live_list_select"
            android:scrollbars="none"></ListView>
    </LinearLayout>


</RelativeLayout>